// Admin Page - re-exports from the main admin implementation
// This allows the new routing structure to work with the existing admin code

// For now, we re-export the AdminPage class from the original location
export { AdminPage } from '../../js/pages/admin/admin-page.js'
